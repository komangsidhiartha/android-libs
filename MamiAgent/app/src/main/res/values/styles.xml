<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/apptheme_color</item>
        <item name="colorPrimaryDark">@color/dark_brand_color</item>
        <item name="colorAccent">@color/accent_brand_color</item>
        <item name="android:windowActionBar">true</item>
    </style>

    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/white</item>
        <item name="tabBackground">@color/accent</item>
        <item name="tabSelectedTextColor">@color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--@android:style/Widget.RatingBar-->
    <style name="reviewDetail" parent="@android:style/Widget.RatingBar">
        <item name="colorControlNormal">@color/gray</item>
        <item name="colorControlActivated">@color/apptheme_color</item>
    </style>

    <style name="TabLayoutTextStyle">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ProgressBarOrange" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@color/orange</item>
    </style>

</resources>
